export enum ShippingType {
    Manual = 'manual', // Person goes to pick it up themselves
    DeliverHome = 'deliverhome', // The package is delivered to their house
    PickupStop = 'pickupstop' // The package should be delivered to a pick up machine
}